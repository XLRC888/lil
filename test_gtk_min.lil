include gtk

&gtk|window win "Mini Test" 300 200
&gtk|on win destroy closed
&gtk|show win

print "waiting..."
&gtk|wait
print "back from wait, closed=" closed

if closed == 1 {
    print "closed is 1, exiting"
} else {
    print "closed is not 1, value: '" closed "'"
}
