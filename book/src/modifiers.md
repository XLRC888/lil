# Modifiers

Modifiers use the `?` prefix to change global behavior. Currently there is one modifier.

## Modifiers

- [Undefined Default](undefined_default.md) — change what undefined variables return
